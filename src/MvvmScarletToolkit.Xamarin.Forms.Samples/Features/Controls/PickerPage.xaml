<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MvvmScarletToolkit.Xamarin.Forms.Samples.Features.Controls.PickerPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MvvmScarletToolkit.Xamarin.Forms.Samples.Features.Controls"
             Title="Picker">

    <ContentPage.BindingContext>
        <controls:PickerViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Label HorizontalOptions="CenterAndExpand"
                   Text="This illustrates on how to use the picker control"
                   VerticalOptions="CenterAndExpand" />

            <Picker ItemDisplayBinding="{Binding}"
                    ItemsSource="{Binding Items}"
                    SelectedItem="{Binding SelectedItem}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
