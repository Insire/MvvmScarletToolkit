<UserControl x:Class="DemoApp.DataContextSchenanigans"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:DemoApp"
             DataContextChanged="UserControl_DataContextChanged"
             Loaded="UserControl_Loaded">

    <!--  DataContext DataContextSchenanigansViewModel  -->

    <UserControl.Resources>
        <DataTemplate x:Key="DemoItemTemplate" DataType="{x:Type local:DemoItem}">
            <StackPanel Orientation="Horizontal">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Loaded">
                        <i:InvokeCommandAction Command="{Binding LoadCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <Button Command="{Binding RefreshCommand}" Content="Refresh" />

                <TextBlock Text="{Binding DisplayName}">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Foreground" Value="Gray" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Margin" Value="5,0" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                    <Setter Property="Foreground" Value="Blue" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="Foreground" Value="Red" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
                 ItemTemplate="{StaticResource DemoItemTemplate}"
                 ItemsSource="{Binding Items}" />

        <ContentControl Grid.Column="2"
                        VerticalAlignment="Top"
                        Content="{Binding SelectedItem}"
                        ContentTemplate="{StaticResource DemoItemTemplate}" />
    </Grid>
</UserControl>
