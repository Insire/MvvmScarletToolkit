<UserControl x:Class="MvvmScarletToolkit.SnakeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:MvvmScarletToolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>
        <local:SnakeManager x:Key="Manager" />

        <DataTemplate DataType="{x:Type local:SnakeHead}">
            <Rectangle Width="16"
                       Height="16"
                       Fill="Green" />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:SnakeSegment}">
            <Rectangle Width="16"
                       Height="16"
                       Fill="GreenYellow" />
        </DataTemplate>
    </UserControl.Resources>
    <WrapPanel>
        <Viewbox>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding LoadCommand, Source={StaticResource Manager}, diag:PresentationTraceSources.TraceLevel=High}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ItemsControl Width="{Binding UpperBoundX, Source={StaticResource Manager}}"
                          Height="{Binding UpperBoundY, Source={StaticResource Manager}}"
                          Background="LightGray"
                          ItemsSource="{Binding BoardPieces, Source={StaticResource Manager}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding CurrentPosition.X}" />
                        <Setter Property="Canvas.Top" Value="{Binding CurrentPosition.Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Viewbox>

        <Grid Margin="5"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="5" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="5" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0"
                    Grid.Column="2"
                    Width="32"
                    Height="32"
                    Command="{Binding MoveNorthCommand, Source={StaticResource Manager}}"
                    Content="North" />
            <Button Grid.Row="4"
                    Grid.Column="2"
                    Width="32"
                    Height="32"
                    Command="{Binding MoveSouthCommand, Source={StaticResource Manager}}"
                    Content="South" />
            <Button Grid.Row="2"
                    Grid.Column="0"
                    Width="32"
                    Height="32"
                    Command="{Binding MoveWestCommand, Source={StaticResource Manager}}"
                    Content="West" />
            <Button Grid.Row="2"
                    Grid.Column="4"
                    Width="32"
                    Height="32"
                    Command="{Binding MoveEastCommand, Source={StaticResource Manager}}"
                    Content="East" />

            <Button Grid.Row="2"
                    Grid.Column="2"
                    Width="32"
                    Height="32"
                    Command="{Binding PlayCommand, Source={StaticResource Manager}}">
                <Button.Style>
                    <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State, Source={StaticResource Manager}}" Value="{x:Static local:GameState.None}">
                                <Setter Property="Content" Value="Play" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State, Source={StaticResource Manager}}" Value="{x:Static local:GameState.Running}">
                                <Setter Property="Content" Value="Pause" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State, Source={StaticResource Manager}}" Value="{x:Static local:GameState.Paused}">
                                <Setter Property="Content" Value="Resume" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

    </WrapPanel>
</UserControl>
